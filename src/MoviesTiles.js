import './MoviesTiles.css';

function MoviesTiles() {
  return (
    <div className="MoviesTiles">
      array.forEach(element => {
        <MovieTile element='element'/>
      });
    </div>
  );
}

export default MoviesTiles;
